import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {width: '100%', borderRadius: 8, marginBottom: 16},
  button: {width: '100%', paddingVertical: 16},
  text: {textAlign: 'center'},
});

export default styles;
